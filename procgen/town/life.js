/* ------------------------------------------------------------------
 *  Town stage 6: life — establishment names & notable residents.
 *  Consumes ONLY the NAMES stream, landmarks visited in stable order.
 *  (Populated in the overhaul; the tavern name is currently drawn in
 *  plots.js to preserve the legacy stream order.)
 * ------------------------------------------------------------------ */

export function buildLife(ctx) {
    ctx.residents = [];
}
